# Function:  
>Execut a file in simplify binary instruction  
# Build:  
``` bash
(need):gcc Compiler  
  batch:  
      run.bat  
  terminal:  
     'gcc main.c -o bir.exe'
```
# Use
``` bash
bir.exe plugen_table.bin
```
