# Function:  
>Execut a file in simplify binary instruction  
# Build:  
``` bash
(need):gcc Compiler  
  batch:  
      run.bat  
  terminal:  
     'gcc main.c -o main.exe'
```
# Use
``` bash
main.exe plugen_table.bin
```
